import DefaultLayout from "../../common/layouts/default";
import Hero from "../../common/sections/home/Hero/Hero";

const Home = () => {
   return (
      <DefaultLayout>
         <Hero />
      </DefaultLayout>
   );
};

export default Home;
